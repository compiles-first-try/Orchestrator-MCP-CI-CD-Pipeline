{
  "name": "@rpa-platform/github-action",
  "version": "0.0.0",
  "private": true,
  "description": "Composite GitHub Action that posts to the rpa-platform API. No build output — action.yml is the contract.",
  "scripts": {
    "build": "echo 'github-action has no compile step'",
    "test": "echo 'github-action has no automated tests'",
    "typecheck": "echo 'github-action has no typecheck'"
  }
}
