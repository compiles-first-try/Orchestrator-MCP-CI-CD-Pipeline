{
  "name": "@rpa-platform/seed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@rpa-platform/db": "workspace:*",
    "@rpa-platform/shared": "workspace:*",
    "drizzle-orm": "^0.33.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.0",
    "typescript": "^5.5.3",
    "vitest": "^2.0.2"
  }
}
