{
  "name": "rpa-project-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "CI/CD deploy tooling for a UiPath RPA project — reads Data/Config.xlsx and provisions Orchestrator.",
  "scripts": {
    "deploy": "tsx scripts/deploy.ts",
    "validate": "tsx scripts/deploy.ts --validate",
    "make-config": "tsx scripts/make-config-xlsx.ts"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
